# Stub — Task 15 replaces with full handoff logic (reasons, persistence).
def handoff_node(deps, state):
    return {"response": "Đã ghi nhận, mình sẽ chuyển nhân viên hỗ trợ.", "should_handoff": True}
